# ============================================================================
# Azure Data Engineering Project - Environment Variables Template
# ============================================================================
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values in .env
# 3. Never commit .env to git (it's in .gitignore)
# ============================================================================

# -----------------------------------------------------------------------------
# General Azure Settings
# -----------------------------------------------------------------------------
AZURE_RESOURCE_GROUP="your-resource-group-name"
AZURE_LOCATION="eastus"
AZURE_LOCATION_SQL="westus2"  # Change if needed

# -----------------------------------------------------------------------------
# Storage Account Settings
# -----------------------------------------------------------------------------
AZURE_STORAGE_ACCOUNT_NAME="yourstorageaccount"  # Must be globally unique, 3-24 chars, lowercase/numbers only
AZURE_STORAGE_ACCOUNT_KEY=""  # Get from: az storage account keys list --account-name <name> --query "[0].value" -o tsv

# -----------------------------------------------------------------------------
# SQL Server & Database Settings
# -----------------------------------------------------------------------------
AZURE_SQL_SERVER_NAME="your-sql-server-name"
AZURE_SQL_DATABASE_NAME="your-database-name"
AZURE_SQL_DATABASE_USER="your-admin-username"
AZURE_SQL_DATABASE_PASSWORD="YourSecurePassword123!"  # Must meet complexity requirements

# Connection string (auto-constructed)
AZURE_SQL_CONNECTION_STRING="Server=tcp:${AZURE_SQL_SERVER_NAME}.database.windows.net,1433;Database=${AZURE_SQL_DATABASE_NAME};User ID=${AZURE_SQL_DATABASE_USER};Password=${AZURE_SQL_DATABASE_PASSWORD};Encrypt=True;Connection Timeout=30;"

# -----------------------------------------------------------------------------
# Data Factory Settings
# -----------------------------------------------------------------------------
AZURE_DATA_FACTORY_NAME="your-data-factory-name"

# -----------------------------------------------------------------------------
# Databricks Settings (for future use)
# -----------------------------------------------------------------------------
# AZURE_DATABRICKS_WORKSPACE_NAME=""
# AZURE_DATABRICKS_HOST=""
# AZURE_DATABRICKS_TOKEN=""
